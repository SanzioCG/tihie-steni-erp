
[functions.send-push-notification]
enabled = true
# Auth funksiya ichida boshqariladi: cron uchun x-cron-secret, frontend uchun getUser.
# Platforma JWT gateway'i keraksiz (va cron chaqiruvini 401 qilardi) — o'chirilgan.
verify_jwt = false
import_map = "./functions/send-push-notification/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/send-push-notification/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/send-push-notification/*.html" ]
